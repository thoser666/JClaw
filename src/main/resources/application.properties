spring.application.name=jclaw
spring.ai.ollama.base-url=http://localhost:11434
spring.ai.ollama.chat.options.model=qwen3:8b
spring.ai.ollama.chat.options.temperature=0.3
spring.ai.ollama.chat.options.num-ctx=8192

server.port=8080

spring.datasource.url=jdbc:h2:file:./data/jclaw
spring.datasource.driver-class-name=org.h2.Driver
spring.sql.init.mode=always

jclaw.agent.max-iterations=8
jclaw.agent.max-history-messages=10
jclaw.agent.plugins.dir=./plugins

# Session-Verwaltung: Reset-Strategien (none | daily | idle)
jclaw.session.reset-mode=none
jclaw.session.reset-at-hour=4
jclaw.session.reset-idle-minutes=60

# MCP-Server (Deny-by-Default): erst mit enabled=true werden die Tools registriert.
jclaw.mcp.enabled=false
# jclaw.mcp.request-timeout=60s
# jclaw.mcp.servers.filesystem.url=http://localhost:8080
# jclaw.mcp.servers.linter.command=npx
# jclaw.mcp.servers.linter.args=-y, @modelcontextprotocol/server-everything

# Web-Werkzeuge (Deny-by-Default): web_fetch (nur erlaubte Domains) + web_search.
jclaw.agent.webtool.enabled=false
# jclaw.agent.webtool.allowed-domains=example.com, docs.spring.io
# jclaw.agent.webtool.search-endpoint=https://api.duckduckgo.com
# jclaw.agent.webtool.fetch-timeout-seconds=10
# jclaw.agent.webtool.max-fetch-bytes=200000
# jclaw.agent.webtool.max-search-results=5

# Tool-Policies (Deny-by-Default): allow = nur diese Tools aktiv, deny = diese sperren (Deny schlägt Allow).
# jclaw.agent.tools.allow=readFile,glob,writeFile
# jclaw.agent.tools.deny=runCommand

# Spawn-Agent (Deny-by-Default): Sub-Agenten für Multi-Agent-Aufgaben.
jclaw.agent.spawnagent.enabled=false
jclaw.agent.spawnagent.max-depth=3

# Hot-Reload (P2-03): Auto-Detect Änderungen an openclaw.json + manuelles config.apply
jclaw.config.hot-reload.enabled=false

# Auth (P2-06): API-Token-Authentifizierung (Deny-by-Default)
jclaw.auth.enabled=false
# jclaw.auth.public-paths=/api/v1/gateway/status,/api/v1/gateway/info

# Hooks (P1-11): Lifecycle-Hooks via HOOK.md-Scripts (Deny-by-Default)
jclaw.hooks.enabled=false
jclaw.hooks.dir=./hooks
jclaw.hooks.script-timeout=30
# jclaw.hooks.allowed-stages=before_agent_run,after_agent_run,before_tool_call,after_tool_call

# Cron-Jobs (P1-12): Wiederkehrende Agent-Jobs (Deny-by-Default)
jclaw.cron.enabled=false
jclaw.cron.interval=60
jclaw.cron.max-retries=3

# Channels (P3-01): Nachrichten-Channel-Abstraktion (Deny-by-Default)
jclaw.channels.enabled=false
jclaw.channels.default-timeout=30
